#include <vector>
#include <algorithm>
#include <iostream>
#include <queue>

using namespace std;

const int N = 200005;
int n;
int a[N];
int prv[N], nxt[N];
bool in[N];


void solve() {
    cin >> n;

    vector<int> arr(n);
    for(int&i:arr) cin >> i;


}


int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    int t; cin >> t;
    while(t--) solve();
}